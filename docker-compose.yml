services:
  verbmaster:
    build: .
    restart: unless-stopped
    volumes:
      - ./verbmaster.db:/app/verbmaster.db
      - /var/www/mp3:/app/mp3s:ro
    environment:
      PORT: 3000
      MP3_PATH: /app/mp3s
    ports:
      - "3001:3000"

